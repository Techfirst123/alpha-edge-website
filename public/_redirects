# SPA fallback for React Router — Cloudflare Pages Functions (functions/api/*,
# functions/uploads/*) already take priority over this for matching paths,
# so this only ever applies to actual frontend routes. Replaces vercel.json's
# rewrites, which only applied on Vercel.
/*    /index.html   200
